## Summary

<!-- What does this PR do? One sentence. -->

## Motivation

<!-- Why is this change needed? Link related issues with "Closes #N" or "Ref #N". -->

## Changes

<!-- Bullet list of what changed. Be specific: file paths, function names, behavior. -->

-

## Test plan

<!-- How did you verify this works? What edge cases did you consider? -->

- [ ] Unit tests pass (`cargo test --lib`)
- [ ] Integration tests pass (`cargo test`)
- [ ] Redis compat tests unaffected (`cargo test -p ferrite-core --test compat`)
- [ ] Clippy clean (`cargo clippy --all-targets -- -D warnings`)
- [ ] Formatted (`cargo fmt --all --check`)

## Notes for reviewers

<!-- Anything that needs extra attention, known limitations, or follow-up items. -->
